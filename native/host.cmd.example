@echo off
REM Generated by install-host.ps1 - do not edit; re-run the installer instead.
REM
REM Two machine-specific values, and nothing else. Everything the host needs
REM beyond these comes from kb_config (env var, then config.json, then default).
REM
REM   {{PYTHON}}   full path to the interpreter
REM   {{SCRIPTS}}  directory holding claude_kb.py, kb_config.py, kb_ingest.py
set "CLAUDE_KB_SCRIPTS={{SCRIPTS}}"
"{{PYTHON}}" -u "%~dp0host.py"
